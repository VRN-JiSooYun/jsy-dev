services:
  dev-ubuntu:
    image: ghcr.io/vrn-jisooyun/vdev-ubuntu:${TAG_NAME}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-ubuntu
    tty: true
    stdin_open: true
    ports:
      - "0.0.0.0:1455:1455"

volumes:
  dev-ubuntu_data:
